#include <stdio.h>
#include <stdarg.h>
#include <string.h>

int _printf(const char *format, ...)
{
	format [f] = ['c','s','%']
	va_list args
	va_starts(args,format);
	for (int i =0; i < format [f];
			if( format [f] == 'c')
			{ char c = va_arg(args,char);
			putchar (c);
			else if (format [f] == 's')
			{while (s[]!= '\0') && 


		
