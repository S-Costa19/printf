#include "main.h"
/**
 * _printf - Printf function
 * @format: format.
 * Return: Printed chars.
 */
int _printf(const char *format, ...)
{
	va_list arg;
	int CHorSTr = strlen(format)
	va_start(arg,
			/
	va_end(arg);
	return ();
}

